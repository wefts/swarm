# Keep CLAUDE.md tracked regardless of broader ignores.
!CLAUDE.md

# Scratch (spike, task notes, experiments).
tmp

# --- Elixir kernel ---
_build
.fetch
/kernel/cover/
/kernel/deps/
/kernel/doc/
/kernel/.elixir_ls/
erl_crash.dump
*.ez
*.beam

# --- Python (ML service + CLI channel) ---
.venv
__pycache__
*.py[cod]
.ruff_cache
.pytest_cache
.mypy_cache

# --- Generated Protobuf stubs (regenerate with `task proto`) ---
_gen
/kernel/lib/swarm/proto/

# --- mise local overrides (machine-specific, not committed) ---
mise.local.toml
.mise.local.toml
