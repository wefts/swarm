# Keep CLAUDE.md tracked regardless of broader ignores.
!CLAUDE.md

# Scratch (spike, task notes, experiments).
tmp

# --- Elixir kernel ---
/kernel/_build/
/kernel/cover/
/kernel/deps/
/kernel/doc/
/kernel/.fetch
/kernel/.elixir_ls/
erl_crash.dump
*.ez
*.beam

# --- Python ML ---
/ml/.venv/
__pycache__/
*.py[cod]
.ruff_cache/
.pytest_cache/
.mypy_cache/

# --- Generated Protobuf stubs (regenerate with `task proto`) ---
/ml/src/swarm_ml/_gen/
/kernel/lib/swarm/proto/

# --- mise local overrides (machine-specific, not committed) ---
mise.local.toml
.mise.local.toml
