"""Swarm CLI channel — a thin client over the kernel Core API (Domain 11).

Renders answers and accepts input; the kernel owns cognition and the
single-voice rule. Adding another channel later needs no kernel change.
"""
